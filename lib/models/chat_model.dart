class ChatModel{
  final String ?firstName;
  final String ?lastName;
  final String ?chat;
  final bool ? isOnline;

 const ChatModel({
     this.firstName,
     this.lastName,
     this.chat,
     this.isOnline,
  });
}